import { createTheme } from "@mui/material";
//  Create a theme instance with custom typography
const theme = createTheme({
  typography: {
    fontFamily: '"Josefin Slab", serif',
  },
});

export default theme;
